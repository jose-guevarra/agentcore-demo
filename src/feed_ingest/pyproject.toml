[project]
name = "feed_ingest"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Jose Guevarra", email = "jagx4669@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "feedparser>=6.0.14",
]

[dependency-groups]
dev = [
    "boto3>=1.43.71",
    "pytest>=9.1.1",
]

[project.scripts]
#chat-agent = "chat_agent:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"

[tool.uv]
package = false
