[project]
name = "webapp"
version = "0.1.0"
description = "Streamlit chat UI for the AgentCore demo, backed by Cognito login"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "streamlit>=1.50.0",
    "boto3>=1.43.56",
    "requests>=2.32",
    "python-dotenv>=1.2.2",
]

[tool.uv]
package = false
