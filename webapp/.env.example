# Copy to .env and fill in with values from:
#   terraform -chdir=../infra/acdemo output
#   aws sts get-caller-identity --query Account --output text

AWS_REGION=us-east-1
ACCOUNT_ID=
AGENTCORE_RUNTIME_ID=
COGNITO_CLIENT_ID=
