# flush_long_term_memory.sh Config file
# Copy this file to .env.flush_long_term_memory and replace with your actual values

AWS_REGION="us-east-1"

# terraform -chdir=infra/acdemo output memory_id
MEMORY_ID=""

# One actor = one user (see webapp/agent_client.py's actor_id_for_user). This
# flushes every long-term preference record extracted for that user (favorite
# team, favorite player, etc.), across all of their chats -- for short-term
# conversation history instead, use flush_memory.sh/flush_all_chats.sh.
ACTOR_ID="webapp-XXXXXXXX-XXXX-XXXX-XXXX-XXXXXXXXXXXX"
