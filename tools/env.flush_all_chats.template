# flush_all_chats.sh Config file
# Copy this file to .env.flush_all_chats and replace with your actual values

AWS_REGION="us-east-1"

# terraform -chdir=infra/acdemo output memory_id
MEMORY_ID=""

# One actor = one user, shared across all of their chats (see
# webapp/agent_client.py's actor_id_for_user). This flushes every session
# (every chat) that actor has -- for a single chat, use flush_memory.sh instead.
ACTOR_ID="webapp-XXXXXXXX-XXXX-XXXX-XXXX-XXXXXXXXXXXX"
