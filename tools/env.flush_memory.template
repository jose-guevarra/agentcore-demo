# flush_memory.sh Config file
# Copy this file to .env.flush_memory and replace with your actual values

AWS_REGION="us-east-1"

# terraform -chdir=infra/acdemo output memory_id
MEMORY_ID=""

# The webapp uses session_id_for_user(sub) = "webapp-<cognito sub>" as both
# actor_id and session_id (see webapp/agent_client.py), so these two match.
ACTOR_ID="webapp-XXXXXXXX-XXXX-XXXX-XXXX-XXXXXXXXXXXX"
SESSION_ID="webapp-XXXXXXXX-XXXX-XXXX-XXXX-XXXXXXXXXXXX"
